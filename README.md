# 🎯 Object Detection System

This project implements an Object Detection system using deep learning to detect and classify objects in images.

---

## 📌 Project Overview

The model detects multiple objects in an image and draws bounding boxes around them with confidence scores.

Example objects detected:
- Person
- Car
- Dog
- Bottle
- etc.

---

## 🛠️ Tech Stack

- Python
- TensorFlow / PyTorch
- OpenCV
- NumPy
- Matplotlib

---

## 🧠 Model Used

Model: YOLOv5 / SSD / Faster R-CNN (edit this)

- Pretrained on COCO dataset
- Fine-tuned on custom dataset (if applicable)

---



---

## ⚙️ Installation

Clone the repository:

```bash
git clone https://github.com/7Vijval/object-detection.git
cd object-detection

pip install -r requirements.txt
python detect.py --image sample.jpg

